# wechat-article Phase 0 依赖
# 装：python3.11 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# LLM 调用
openai==2.29.0

# 环境 / 配置
python-dotenv==1.0.1
PyYAML==6.0.3

# PDF 抽取（文本）+ 图（渲染裁剪：pypdfium2 渲染 + Pillow 裁剪）
pdfplumber==0.11.9
pypdfium2==4.30.0
Pillow==12.2.0

# Markdown 转 HTML
markdown==3.10.2

# DB（继承自 target-running 的栈，方便后续 dashboard 复用）
SQLAlchemy==2.0.36
PyMySQL==1.1.2          # 若用 MySQL；用 SQLite 可不装但留着方便

# Dashboard（Phase 7，服务端渲染，无前端构建）
Flask==3.1.3
gunicorn==26.0.0

# Blog article image publishing
oss2==2.19.1
